# ==============================================================================
# DEPENDENCIAS FIJADAS (lock completo) — Contabilidad RM2
# ------------------------------------------------------------------------------
# Todas las versiones están pineadas al entorno que funcionaba en Streamlit
# Cloud (Python 3.14.6). Build 100% reproducible: ningún rebuild vuelve a tomar
# una versión nueva incompatible (esto evita el "Oh no. Error running app" que
# causó el auto-upgrade de pyarrow 24->25 y websockets 16.0->16.1).
#
# Para actualizar a propósito: cambia la versión, haz push y VERIFICA el log de
# build (Manage app -> logs). Si aparece "run-streamlit.sh: ... Segmentation
# fault", revierte. No subas pyarrow ni librerías nativas sin verificar.
# ==============================================================================

# --- Dependencias directas de la app ---
streamlit==1.59.1
pandas==3.0.3
openpyxl==3.1.5
xlrd==2.0.2

# --- Librerías nativas críticas (causaron el segfault si suben) ---
pyarrow==24.0.0
websockets==16.0
numpy==2.5.1

# --- Transitivas (fijadas para reproducibilidad total) ---
altair==6.2.2
anyio==4.14.1
attrs==26.1.0
blinker==1.9.0
cachetools==7.1.4
certifi==2026.6.17
charset-normalizer==3.4.9
click==8.4.2
et-xmlfile==2.0.0
gitdb==4.0.12
gitpython==3.1.50
h11==0.16.0
httptools==0.8.0
idna==3.18
itsdangerous==2.2.0
jinja2==3.1.6
jsonschema==4.26.0
jsonschema-specifications==2025.9.1
markupsafe==3.0.3
narwhals==2.23.0
packaging==26.2
pillow==12.3.0
protobuf==7.35.1
pydeck==0.9.3
python-dateutil==2.9.0.post0
python-multipart==0.0.32
referencing==0.37.0
requests==2.34.2
rpds-py==2026.6.3
six==1.17.0
smmap==5.0.3
starlette==1.3.1
tenacity==9.1.4
toml==0.10.2
typing-extensions==4.16.0
urllib3==2.7.0
uvicorn==0.51.0
watchdog==6.0.0
