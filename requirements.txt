streamlit>=1.35.0
pandas>=2.0.0
openpyxl>=3.1.0
xlrd>=2.0.1
# Librerías nativas fijadas al set que funcionaba en Cloud (Python 3.14):
# el rebuild subió pyarrow 24->25 y websockets 16.0->16.1 a la vez y la app
# empezó a caer con "Segmentation fault" ("Oh no. Error running app").
# NO subir estas versiones sin verificar el redeploy en Cloud.
pyarrow==24.0.0
websockets==16.0
